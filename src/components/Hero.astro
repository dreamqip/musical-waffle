---
import { Image } from 'astro:assets';
import Button from './Button.astro';

import image from '../assets/hero.png';
---

<div
  class="mx-auto max-w-[1440px] overflow-hidden rounded-r-[32px] bg-stromboli-700 transition-all duration-200 lg:rounded-[64px] xl:rounded-[128px]"
  id="hero"
>
  <div class="container relative">
    <div class="grid place-items-center lg:grid-cols-2 lg:items-end lg:gap-8">
      <div
        class="grid items-center py-12 text-left sm:text-center md:pt-20 lg:items-start lg:text-left"
      >
        <h1 class="max-w-2xl text-4xl font-bold text-stromboli-50 md:text-6xl">
          Удвою Ваш доход и верну страсть к жизни!
        </h1>
        <div
          class="mt-8 max-w-lg text-left text-lg font-medium text-reef-200 sm:mx-auto sm:text-center lg:mx-0 lg:text-left"
        >
          <i>
            Вы снова почувствуете прилив энергии и желание творить. Ваша жизнь
            станет ярче, а возможности —безграничными! Со 100% гарантией по
            договору.
          </i>
        </div>

        <Button href="#contact" class="mx-0 mt-8 sm:mx-auto sm:mt-12 lg:mx-0">
          Записаться на консультацию
        </Button>
      </div>
      <Image
        class="min-h-[400px] object-cover object-center"
        src={image}
        densities={[1.5, 2]}
        quality="high"
        width={528}
        height={564}
        loading="eager"
        fetchpriority="high"
        alt="Денис Кравчук"
      />
    </div>
  </div>
</div>
